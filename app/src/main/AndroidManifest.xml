<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fz.imageloader.demo">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.fz.imageloader.MyApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.ImageLoader">
        <activity
            android:name="com.fz.imageloader.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.fz.imageloader.activity.CropCircleActivity" />
        <activity android:name="com.fz.imageloader.activity.RotateActivity" />
        <activity android:name="com.fz.imageloader.activity.BlurActivity" />
        <activity android:name="com.fz.imageloader.activity.GrayScaleActivity" />
        <activity android:name="com.fz.imageloader.activity.GifActivity" />
    </application>

</manifest>